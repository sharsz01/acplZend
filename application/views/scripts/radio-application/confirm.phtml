<style>
    .listener{
        //border: 1px solid black;
        clear: left;
    }

    label{
        //display: inline-block;
    }

    .line{
        display: inline-block;
    }

    label.key{
        display: inline-block;
        width: 200px;
        float: left;
        font-weight: bold;
        text-align: right;
    }

    label.value{
        width: 300px;
        margin-left: 20px;
        float: left;
        //display: inline-block;
    }
</style>


<?php
session_start();

// get session variables from form
$listenerData = $_SESSION['data']['postData']['listener'];
$contactData = $_SESSION['data']['postData']['contact'];
$otherInfoData = $_SESSION['data']['postData']['otherInfo'];
$statementData = $_SESSION['data']['postData']['statement'];

echo "<h2>Review Your Information and Submit</h2>";


// ================================================= Listener
echo "<div class='listener'>";
echo "<h2>Listener</h2>";
foreach ($listenerData as $key => $value) {
    //echo "<div class='listener'>" . $i . "</div>";
    echo "<div class='line'><label class='key'>$key : </label> <label class='value'>$value</label></div>";
}
echo "</div>";
// ================================================= Contact
echo "<div class='contact'>";
echo "<h2>Contact</h2>";
foreach ($contactData as $key => $value) {
    //echo $i . "<br />";
    echo "<div class='line'><label class='key'>$key : </label> <label class='value'>$value</label></div>";
}
echo "</div>";
// ================================================= Other Info
echo "<div class='otherInfo'>";
echo "<h2>Other Info</h2>";
foreach ($otherInfoData as $key => $value) {
    //echo $i . "<br />";
    echo "<div class='line'><label class='key'>$key : </label> <label class='value'>$value</label></div>";
}
echo "</div>";
// ================================================= Statement
echo "<div class='statement'>";
echo "<h2>Statement</h2>";
foreach ($statementData as $key => $value) {
    //echo $i . "<br />";
    echo "<div class='line'><label class='key'>$key : </label> <label class='value'>$value</label></div>";
}
echo "</div>";
// =================================================


$this->form->setAction($this->url());
echo $this->form;

